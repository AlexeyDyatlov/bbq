<% errors = model.errors %>

<% if errors.any? %>
  <div class="alert alert-danger alert-dismissible fadeout">
    <button type="button" class="close" data-dismiss="alert">&times;</button>
    <ul>
      <% errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
